@if (clerk is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <ClerkForm Clerl="clerk" 
                         OnValidSubmit="EditAsync" 
                         ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}